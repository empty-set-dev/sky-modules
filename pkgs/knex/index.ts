import pkg from 'knex'

const {
    Client,
    ColumnBuilder,
    Knex,
    KnexTimeoutError,
    QueryBuilder,
    SchemaBuilder,
    TableBuilder,
    ViewBuilder,
} = pkg

export default pkg

export {
    Client,
    ColumnBuilder,
    Knex,
    KnexTimeoutError,
    QueryBuilder,
    SchemaBuilder,
    TableBuilder,
    ViewBuilder,
}
