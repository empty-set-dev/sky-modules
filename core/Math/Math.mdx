### *function* Math.minmax(value: number, min: number, max: number): number
```ts
const x = -9
Math.minmax(x, 0, 100) // 0
```

### *function* Math.randomBetween(from: number, to: number): number
```ts
Math.randomBetween(0, 10) // 0 - 10
```

### *function* Math.roundedRandomBetween(from: number, to: number): number
```ts
Math.roundedRandomBetween(0, 10) // rounded 0 - 10
```