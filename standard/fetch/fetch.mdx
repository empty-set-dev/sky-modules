import Header from '../../docs/Nav.js'

<Header root='../../' selected="standard/fetch" />

## fetch.json
```typescript
const json = await fetch.json() // json
```

## fetch.text
```typescript
const text = await fetch.text() // text
```