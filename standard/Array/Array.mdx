import Header from '../../docs/Nav.js'

<Header root='../../' selected="standard/Array" />

## remove
``` typescript
const a = {}
const b = {}
const c = {}
const array = [a, b, c]
const isRemoved = array.remove(a)
// isRemoved - true
// array - [b, c]
```

## ArrayIndex
```typescript
type ArrayIndex = number
```