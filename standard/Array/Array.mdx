### *method* Array`<T>`.last(): T
``` typescript
const a = {}
const b = {}
const c = {}
const array = [a, b, c]
const lastElement = array.last() // c
```

### *method* Array`<T>`.remove(elem: T): boolean
``` typescript
const a = {}
const b = {}
const c = {}
const array = [a, b, c]
const isRemoved = array.remove(a)
// isRemoved - true
// array - [b, c]
```

### *type* Array.Index
```ts
const x: Array.Index // number
```