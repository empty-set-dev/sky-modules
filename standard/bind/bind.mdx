import Nav from 'sky/docs'

<Nav root='../../' selected="standard/bind" />

```typescript
class Foo {
    x = 42

    @bind
    foo() {
        console.log(this.x)
    }
}

const foo = new Foo()
const foofoo = foo.foo
foofoo() // 42
```