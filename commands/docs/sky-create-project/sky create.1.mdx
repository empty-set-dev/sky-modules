### Sky create project
Requires: pnpm, bun (globally)

Install bun globally
```sh
pnpm i bun --global
```

Create project folder, init package.json, get and run create command
```sh
mkdir ${PROJECT_FOLDER}
cd ${PROJECT_FOLDER}
pnpm init
sky create
```
