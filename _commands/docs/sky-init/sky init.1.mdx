### init all
```sky init```

### sky.config.ts
```sh
sky init sky-config
```

```ts
export default {
    title: 'App Title',
    apps: {
        'app name': {
            path: '.',
            target: 'web',
            public: 'public',
        },
    },
    examples: {
        name: {
            path: 'example path',
        },
    },
    modules: {
        name: {
            path: 'module path',
        },
    },
    scripts: {
        some: 'some command',
    },
}
```

### tsconfigs
```sh
sky init tsconfigs
```

### .gitignore
```sh
sky init gitignore
```

### package.json
```sh
sky init package
```

### packages and lint configs
```sh
sky init packages
```

