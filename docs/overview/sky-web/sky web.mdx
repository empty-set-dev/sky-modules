import Nav from 'docs'

<Nav selected="docs/overview/sky-web" />

```sh
sky browser dev [app-name] --port 3000 --open
```
```sh
sky browser build [app-name]
```
```sh
sky browser start [app-name] --port 3000 --open
```
