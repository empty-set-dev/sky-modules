import '@sky-modules/canvas'

export class Line {
    @bind
    render = function Line(): JSX.Element {
        return (
            <canvas>
                {/* <mesh>
                    <circle x={0} y={0} radius={20} />
                </mesh> */}
            </canvas>
        )
    }
}
