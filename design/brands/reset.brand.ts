import Brand from '../Brand'

import foundation from './reset.brand.foundation'
import layout from './reset.brand.layout'
import semantic from './reset.brand.semantic'

export default {
    name: 'sky.reset',
    foundation,
    semantic,
    layout,
} satisfies Brand
