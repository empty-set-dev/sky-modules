import Header from '../../docs/Header.tsx'

<Header root='../../' selected="standard/bind" />

```typescript
class Foo {
    x = 42

    @bind
    foo() {
        console.log(this.x)
    }
}

const foo = new Foo()
const foofoo = foo.foo
foofoo() // 42
```../../docs/Header.js